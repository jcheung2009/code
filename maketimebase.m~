function [timebase] = maketimebase(cbin)
    %
    % generates time axis given a cbin file
    % mnm, 15 april 2009
    %
    
    [thesong sampRate] = ReadCbinFile(cbin);
    length(thesong);
    
    dt = 1/sampRate;
    timebase = [dt:dt:(length(thesong)-1)*dt];
    
    length(timebase)