function vals=getonsets(notMat,syll)
%
% mnm, 28 april 2009
%

if(isempty(syll))
    syll = '';
end

labels = notMat.labels;
    
for index=1:length(labels);
    thesyll = labels(index);
    

    
end