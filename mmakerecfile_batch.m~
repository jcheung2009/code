function mboostwavfiles_batch(batch)
%
%
% mbostwavfiles_batch(batch)
% boosts amplitude of wav files by 1e3 so ev functions plot the 
%
%

files = load_batchf(batch);
for i=1:length(files);
    
    fn=files(i).name;
	if (~ischar(fn));
		break;
    end
    mmakerecfile(fn);
end